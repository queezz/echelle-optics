# echelle_optics

Spectrometer optics, dispersion, and geometry utilities

## Quickstart

```bash
python -m pip install -e ".[dev]"
pytest
```

## Docs

```bash
python -m pip install -e ".[docs]"
mkdocs serve
```
